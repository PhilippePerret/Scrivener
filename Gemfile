source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# ruby '2.3.3'

source 'https://rubygems.org' do
  # gem 'yaml' # fait partie de la librairie ruby
  gem 'curses'
  gem 'plist', '>=3.4.0'
  gem 'rake'
end
